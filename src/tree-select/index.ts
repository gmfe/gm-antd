// 文档入口:re-export compat 层的 TreeSelect 垫片(含 TreeNode / SHOW_ALL / SHOW_PARENT / SHOW_CHILD 静态成员)。
export { default } from '../compat/TreeSelect';
export type { TreeSelectProps } from '../compat/TreeSelect';
